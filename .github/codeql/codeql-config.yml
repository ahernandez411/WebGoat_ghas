name: Custom JavaScript CodeQL Config

disable-default-queries: true # This property should always be at the root of the file

ignore-paths:
- src/**/libs/**

queries:
- uses: ./codeql-custom-queries/javascript

languages:
- javascript
